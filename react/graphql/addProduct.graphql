mutation AddProduct($product: ProductInput!) {
    addProduct(product: $product) {
        id
        refundId
        skuId
        skuName
        unitPrice
        quantity
        totalPrice
        goodProducts
        status
    }
}
