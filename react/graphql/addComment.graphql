mutation AddComment($comment: CommentInput!) {
    addComment(comment: $comment) {
        id
        refundId
        status
        comment
        visibleForCustomer
        submittedBy
        dateSubmitted
    }
}
