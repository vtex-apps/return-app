query getPublicSchema($dataEntity: String!, $schema: String!) {
    documentPublicSchema(dataEntity: $dataEntity, schema: $schema)
    @context(provider: "vtex.store-graphql") {
        schema
    }
}
