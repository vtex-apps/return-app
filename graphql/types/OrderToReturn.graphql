type OrderToReturnList {
  list: [OrderToReturnSummary]
  paging: Pagination
}

type OrderToReturnSummary {
  orderId: String!
  creationDate: String!
}

type Pagination {
  total: Int!
  pages: Int!
  currentPage: Int!
  perPage: Int!
}
